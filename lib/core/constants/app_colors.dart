import 'package:flutter/material.dart';

mixin SameColors {
  static const operatorBtnColor = Color.fromRGBO(209, 71, 2, 1);
}
mixin DarkColors {
  static const scaffoldBgColor = Color(0xff000000);
  static const numBtnColor = Color(0xffffffff);
  static const iconColor = Color.fromRGBO(255, 255, 255, 0.541);
}

mixin LightColors {
  static const scaffoldBgColor = Color(0xffffffff);
  static const numBtnColor = Color(0xff000000);
  static const iconColor = Color.fromRGBO(158, 158, 158, 1);
}
